{
  "name": "@zirtan/ai-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Zirtan AI — Claude API gateway (uygulama API anahtarını bündüllemez)",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "tsc -p tsconfig.json && node --watch dist/index.js",
    "check": "tsc --noEmit -p tsconfig.json",
    "syntax": "npm run build && node --check dist/index.js && node --check dist/prompt.js && node --check dist/tools.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.124.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.9.0"
  },
  "main": "dist/index.js"
}
