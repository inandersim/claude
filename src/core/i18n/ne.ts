import { en } from './en';
import type { Translations } from './tr';

/**
 * ne — çeviri henüz yapılmadı; İngilizce sözlük kullanılır.
 * Tam çeviri için translator ajanı bu dosyayı ve modules/locales/ne/ dosyalarını üretir.
 */
export const ne: Translations = en;
